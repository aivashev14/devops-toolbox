PORT=4000
DATABASE_URL=postgres://devops:devops_password@localhost:5432/devops_toolbox
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1d
# Optional. Usually not needed because frontend traffic uses same-origin /api.
# CORS_ORIGIN=https://your-domain.example
